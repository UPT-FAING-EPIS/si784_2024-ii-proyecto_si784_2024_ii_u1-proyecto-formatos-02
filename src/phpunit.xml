<?xml version="1.0" encoding="UTF-8"?>
<phpunit>
    <coverage>
        <include>
            <directory>./src</directory> <!-- Asegúrate de que apunta a la carpeta correcta -->
        </include>
        <exclude>
            <directory>./tests</directory> <!-- Excluye el directorio de pruebas, si es necesario -->
        </exclude>
    </coverage>
    <testsuites>
        <testsuite name="My Test Suite">
            <directory>./tests</directory>
        </testsuite>
    </testsuites>
</phpunit>
